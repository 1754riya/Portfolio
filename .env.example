# Environment Variables for Portfolio App

# GitHub Personal Access Token (required for contribution calendar)
# To get your token:
# 1. Go to GitHub Settings > Developer settings > Personal access tokens
# 2. Generate new token (classic)
# 3. Select 'user' scope for accessing public profile data
# 4. Copy the token and paste it below (remove the # and space)
# REACT_APP_GITHUB_TOKEN=your_github_token_here

# Note: Keep this file secure and never commit it to version control
# Add .env to your .gitignore file